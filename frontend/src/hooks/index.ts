/**
 * Hooks Barrel Export
 *
 * Central export point for all custom React hooks used in ScamAware Jersey.
 * Import hooks from this file for cleaner import statements.
 *
 * @example
 * import { useFormValidation } from '@/hooks';
 */

export {
  useFormValidation,
  type ValidationRule,
  type FieldState,
  type UseFormValidationOptions,
  type UseFormValidationReturn,
  type RegisterReturn,
} from "./useFormValidation";
