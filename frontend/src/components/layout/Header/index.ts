/**
 * Header Component Barrel Export
 *
 * Re-exports the Header component and its types for cleaner imports.
 *
 * @example
 * // Named import
 * import { Header } from '@/components/layout/Header';
 *
 * @example
 * // Type imports
 * import type { HeaderProps, NavItem } from '@/components/layout/Header';
 *
 * @example
 * // Default import
 * import Header from '@/components/layout/Header';
 */

export { Header, default } from "./Header";
export type { HeaderProps, NavItem } from "./Header";
