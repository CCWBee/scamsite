# ==============================================================================
# ScamAware Jersey - Root Environment Configuration
# ==============================================================================
#
# This file contains shared environment variables used by docker-compose and
# inherited by all services. Copy this file to `.env` and fill in your values.
#
# IMPORTANT: Never commit the actual `.env` file to version control.
#            Only this `.env.example` template should be committed.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual values
#   docker-compose up
#
# ==============================================================================

# ==============================================================================
# PROJECT CONFIGURATION
# ==============================================================================

# Docker Compose project name
# Used as a prefix for container names and networks
# Format: lowercase alphanumeric with hyphens
# Default: scamaware
COMPOSE_PROJECT_NAME=scamaware

# ==============================================================================
# ENVIRONMENT SETTINGS
# ==============================================================================

# Node.js environment for frontend service
# Valid values: development, staging, production
# - development: enables hot reloading, verbose logging, source maps
# - staging: production-like but with additional logging
# - production: optimized builds, minimal logging
# Default: development
NODE_ENV=development

# Python environment for guardrails service
# Valid values: development, staging, production
# - development: enables debug mode, auto-reload, verbose logging
# - staging: production-like but with additional logging
# - production: optimized, minimal logging, stricter security
# Default: development
PYTHON_ENV=development

# ==============================================================================
# SERVICE PORTS (Optional - Override defaults)
# ==============================================================================

# Frontend service port
# Format: valid port number (1-65535)
# Default: 3000
# FRONTEND_PORT=3000

# Guardrails API service port
# Format: valid port number (1-65535)
# Default: 8000
# GUARDRAILS_PORT=8000

# Ollama service port
# Format: valid port number (1-65535)
# Default: 11434
# OLLAMA_PORT=11434

# ==============================================================================
# DOCKER SETTINGS (Optional)
# ==============================================================================

# Docker restart policy
# Valid values: no, always, on-failure, unless-stopped
# Default: unless-stopped
# RESTART_POLICY=unless-stopped

# ==============================================================================
# LOGGING (Optional)
# ==============================================================================

# Global log level override
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO
